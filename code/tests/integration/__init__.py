# Integration tests -- require Docker infrastructure (3 strfry relays + 2 LND nodes).
# Run: pytest tests/integration/ -m integration -v
# Skip automatically via docker_env fixture if Docker is unavailable.
